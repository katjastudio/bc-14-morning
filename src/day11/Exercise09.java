package day11;

 /* public class Exercise09 {
  public static void main(String[] args) {
        //9. Given two int values, return whichever value is larger.
        // However if the two values have the same remainder when divided by 5,
        // then the return the smaller value. However, in all cases,
        // if the two values are the same, return 0. Note: the % "mod" operator computes the remainder, e.g. 7 % 5 is 2.

       //System.out.println(maxMod5(2, 3)); //→ 3
      //System.out.println(maxMod5(6, 2)); //→ 6
       //System.out.println(maxMod5(3, 2)); //→ 3

     //   System.out.println(blueTicket(9, 1, 0));// → 10
      //  System.out.println(blueTicket(9, 2, 0));//→ 0
      //  System.out.println(blueTicket(6, 1, 4));// → 10


    }

    private static int blueTicket(int a, int b, int ic) {
    }


    private static int maxMod5(int i, int i1) {
        if (i == i1) {
            return 0;
        } else if (i % 5 == 0 && i1 % 5 == 0) {
            return Math.min(i,i1);

        } else {
            return Math.max(i, i1);
        }
    }
}

   */